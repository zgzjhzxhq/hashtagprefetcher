numIters = 10;
sampleSize = [1:9,10:10:90,100:100:800];

TData = csvread('0-1Train1.csv');

for iters = 1:numIters
    for i = 1:size(sampleSize)
        ind = randperm(800);
        t = TData(ind(1:step*i), :);
        
